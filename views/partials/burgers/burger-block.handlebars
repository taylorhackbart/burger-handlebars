<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{this.id}}" data-newdevour="{{devour}}">
		{{#if devour}}DEVOUR{{else}}GONE{{/if}}
	</button>
	<button class="delete-burger" data-id="{{this.id}}"> DELETE
	</button>
</li>
